<!-- RTL -->
# ארכיטקטורת מערכת — TactiTok

> **גרסה:** 0.2
> **סטטוס:** טיוטה
> **עדכון אחרון:** 2026-03-07
> **מסמך קודם:** `product/03_mvp-spec.md`
> **מסמך הבא:** `product/05_data-model.md`
> **יומן שינויים:** v0.2 — תוקנה טופולוגיית מכשיר קצה: Linux VM מקומי + פרוקסי מטמון Docker מחליף מודל דפדפן-ישיר-לענן.

---

## 1. מטרת מסמך זה

מסמך זה מגדיר את הארכיטקטורה המינימלית הנדרשת לממש את TactiTok MVP. הוא עונה על:

- מהם רכיבי המערכת העיקריים ומה אחריותו של כל אחד
- מה רץ על מכשיר הקצה, מה רץ על השרת, ומה חיצוני
- כיצד תוכן, מטה-נתונים והורדות מקומיות מטופלים ברמה גבוהה
- כיצד המערכת מתנהגת בקישוריות לסירוגין
- מהם הממשקים העיקריים בין רכיבים
- אילו החלטות ארכיטקטורה נדחות בכוונה

כל מסמך נגזר (מודל נתונים → חוזה API → תכנית משלוח) חייב להיות עקבי עם ארכיטקטורה זו.

---

## 2. יעדי ארכיטקטורה

| # | יעד | נימוק |
|---|-----|-------|
| AG1 | **הארכיטקטורה הפשוטה ביותר שישימה** | 3 מפתחי סטודנטים, 8–10 שבועות; מזעור רכיבים נעים |
| AG2 | **פונקציונלית קצה-לקצה** | העלאה → קטלוג → סטרימינג/עיון → הורדה → אופליין חייבים לעבוד |
| AG3 | **מוכנה-להמשך** | הפרדת שכבות נקייה כך שצוות ממשיך יכול להרחיב ללא שכתובים |
| AG4 | **stack טכנולוגי יחיד** | TypeScript בכל מקום (frontend + backend) מפחית עומס קוגניטיבי |
| AG5 | **מודע-לאופליין מהיום הראשון** | פרוקסי קצה + חבילת SPA מקומית מעוצבים לתוך הארכיטקטורה, לא מוסרים עליה |
| AG6 | **מותאם-לדמו** | אתחול מהיר של וידאו, גלילה חלקה, אופליין אמין — אלו מניעים את בחירות הארכיטקטורה |

---

## 3. הגדרות / מונחים

נלקחו ממסמכים קודמים. תוספות ספציפיות לארכיטקטורה:

| מונח | הגדרה |
|------|-------|
| **שרת API** | תהליך Node.js + Express ב-VM הענן המשרת נקודות קצה REST הן לפרוקסי הקצה והן לפורטל הניהול |
| **הפשטת אחסון** | ממשק פנימי (קרא/כתוב/מחק לפי content-id) המנתק את ה-API מה-backend הפיזי של האחסון |
| **פרוקסי קצה** | פרוקסי הפוך עם מטמון nginx הרץ ב-Docker container ב-Linux VM של מכשיר הקצה; משרת את Edge SPA מקומית ומפרוקסי + שומר כל בקשות API/תוכן לשרת הענן במטמון |
| **פרוקסי תוכן** | תפקיד שרת ה-API בהגשת קבצי תוכן עם headers HTTP נכונים (range requests, cache control, CORS) |
| **חבילת SPA** | ה-HTML/JS/CSS הסטטי המרכיב את יישום הלקוח; נבנה בזמן CI/deploy ונארז בתמונת Docker של פרוקסי הקצה |
| **Monorepo** | מאגר אחד המכיל גם קוד frontend וגם backend, משתף טיפוסי TypeScript ו-tooling בנייה |
| **Docker container קצה** | Docker container ב-Linux VM של מכשיר הקצה שארוז nginx + חבילת Edge SPA; מספק הגשה מקומית ויכולת אופליין |

---

## 4. מניעי ארכיטקטורה

אילוצים ודרישות אלו מעצבים כל בחירת ארכיטקטורה:

| מניע | מקור | השפעה על ארכיטקטורה |
|------|------|---------------------|
| 3 מפתחי סטודנטים, 8–10 שבועות | North Star §8 | מספר רכיבים מינימלי; שפה אחת; ללא microservices |
| Chrome קיוסק על Windows, Linux VM עם Docker על אותו מכשיר | North Star §3 + גילוי | Chrome מתחבר ל-localhost; פרוקסי קצה ב-Docker משרת SPA ושומר תוכן ענן במטמון |
| רוחב-פס לא יציב / מוגבל | North Star §3 | פרוקסי קצה שומר תוכן + מטה-נתונים מקומית; מפחית תלות בענן |
| תמיכת אופליין היברידית | North Star §6 | פרוקסי קצה משרת תגובות שמורות כשהענן אינו נגיש; IndexedDB למצב מכשיר |
| פיד וידאו בסגנון TikTok | MVP Spec CAP-2 | השמעה-אוטומטית, scroll-snap, טעינה מוקדמת של הוידאו הבא; מניע גישת משלוח וידאו |
| ~20 מכשירי קצה במקביל | תדריך מוצר §5 | שרת ענן יחיד מספיק; לא נדרש load balancer |
| ללא אימות משתמש (קצה) | תדריך מוצר §4 | ללא auth middleware בנקודות API הפונות-לקצה; אדמין משתמש בסיסמה פשוטה |
| פריסת VM ענן | גבולות מערכת §8 | VM Linux יחיד; כל רכיבי השרת ממוקמים יחד |
| TLS נדרש (ענן) | החלטה D13 | סיום HTTPS בשרת הענן (nginx reverse proxy) |
| MP4 מוכן-להעלאה בלבד | החלטה D14 | ללא צינור עיבוד; אמת ואחסן |
| מכשיר קצה = Windows + Linux VM מקומי | גילוי (ארכיטקטורה) | Docker container ב-Linux VM; Chrome על Windows מתחבר דרך localhost |

---

## 5. סקירת ארכיטקטורה ברמה גבוהה

```
┌──────────────────────────────────────────────────────────────────┐
│                       CLOUD VM (Linux)                            │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │              Node.js + Express (TypeScript)                  │  │
│  │                                                             │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────────────┐    │  │
│  │  │  Content API  │  │  Admin API   │  │ Static Server  │    │  │
│  │  │  (REST)       │  │  (REST)      │  │ (Admin SPA)    │    │  │
│  │  └──────┬───────┘  └──────┬───────┘  └────────────────┘    │  │
│  │         │                 │                                  │  │
│  │  ┌──────┴─────────────────┴──────┐                          │  │
│  │  │       Service Layer            │                          │  │
│  │  │  (content, catalog, category,  │                          │  │
│  │  │   interest, auth)              │                          │  │
│  │  └──────┬─────────────────┬──────┘                          │  │
│  │         │                 │                                  │  │
│  │  ┌──────┴──────┐  ┌──────┴──────┐                          │  │
│  │  │  PostgreSQL  │  │  File Store │                          │  │
│  │  │  (metadata)  │  │  (content)  │                          │  │
│  │  └─────────────┘  └─────────────┘                          │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────┐                                                 │
│  │ TLS (nginx)  │  ← reverse proxy, port 443                     │
│  └──────┬───────┘                                                 │
└─────────┼─────────────────────────────────────────────────────────┘
          │
          │ HTTPS
          │
    ┌─────┴──────────────────────────────────┐
    │                                        │
    │                         ┌──────────────┴──────┐
    │                         │  HQ DESKTOP          │
    │                         │  (modern browser)    │
    │                         │                      │
    │                         │  ┌────────────────┐  │
    │                         │  │ Admin SPA      │  │
    │                         │  │ (React/TS)     │  │
    │                         │  └────────────────┘  │
    │                         └─────────────────────┘
    │
┌───┴──────────────────────────────────────────────────┐
│  EDGE DEVICE ×N  (Windows PC + 10″ screen)            │
│                                                        │
│  ┌─────────────────────┐  ┌─────────────────────────┐ │
│  │   WINDOWS SIDE      │  │   LINUX VM (Docker)     │ │
│  │                     │  │                         │ │
│  │  ┌───────────────┐  │  │  ┌───────────────────┐  │ │
│  │  │ Chrome        │  │  │  │ Docker Container  │  │ │
│  │  │ (kiosk mode)  │──┼──│  │                   │  │ │
│  │  │               │  │  │  │ ┌───────────────┐ │  │ │
│  │  │ localhost:8080 │  │  │  │ │ nginx         │ │  │ │
│  │  │               │  │  │  │ │ (caching      │ │  │ │
│  │  └───────────────┘  │  │  │ │  reverse      │ │  │ │
│  │                     │  │  │ │  proxy)       │ │  │ │
│  │  ┌───────────────┐  │  │  │ └───────┬───────┘ │  │ │
│  │  │ IndexedDB     │  │  │  │         │         │  │ │
│  │  │ (profile,     │  │  │  │ ┌───────┴───────┐ │  │ │
│  │  │  actions,     │  │  │  │ │ SPA bundle    │ │  │ │
│  │  │  download     │  │  │  │ │ (React/TS)    │ │  │ │
│  │  │  records)     │  │  │  │ └───────────────┘ │  │ │
│  │  └───────────────┘  │  │  │                   │  │ │
│  │                     │  │  │ ┌───────────────┐ │  │ │
│  │                     │  │  │ │ Proxy cache   │ │  │ │
│  │                     │  │  │ │ (content +    │ │  │ │
│  │                     │  │  │ │  metadata)    │ │  │ │
│  │                     │  │  │ └───────────────┘ │  │ │
│  │                     │  │  └───────────────────┘  │ │
│  └─────────────────────┘  └─────────────────────────┘ │
└────────────────────────────────────────────────────────┘
```

**שינוי טופולוגיה מרכזי (v0.2):** מכשיר הקצה אינו דפדפן Chrome פשוט המתחבר לענן. כל מכשיר קצה הוא מחשב Windows המריץ Linux VM. בתוך ה-VM, Docker container מריץ פרוקסי מטמון nginx שמבצע:
1. הגשת Edge SPA מעותק מקומי מחובר
2. פרוקסי של כל בקשות API / תוכן לשרת הענן
3. שמירת תגובות מקומית במטמון (גם מטה-נתונים וגם קבצי תוכן)
4. הגשת תגובות שמורות כשהענן אינו נגיש (מצב אופליין)

Chrome על Windows מתחבר ל-`localhost` בלבד. הוא לעולם לא מתחבר ישירות לענן.

---

## 6. רכיבים עיקריים

### 6.1 רשימת רכיבים

| # | רכיב | מיקום | טכנולוגיה | נבנה על ידי הצוות? |
|---|------|-------|-----------|-------------------|
| C1 | **Edge SPA** | מכשיר קצה (Chrome על Windows) | React + TypeScript | ✅ כן |
| C2 | **Admin SPA** | שולחן עבודה מטה (דפדפן) | React + TypeScript | ✅ כן |
| C3 | **שרת API** | VM ענן | Node.js + Express + TypeScript | ✅ כן |
| C4 | **שכבת שירותים** | VM ענן (בתוך שרת API) | מודולי TypeScript | ✅ כן |
| C5 | **PostgreSQL** | VM ענן | PostgreSQL 15+ | ✅ תצורה; לא בניה |
| C6 | **מאגר קבצים** | VM ענן (מערכת קבצים מקומית) | מערכת קבצים OS מאחורי הפשטה | ✅ כן (שכבת הפשטה) |
| C7 | **פרוקסי קצה** | מכשיר קצה (Docker על Linux VM) | nginx caching reverse proxy | ✅ כן (Dockerfile + תצורת nginx) |
| C8 | **סיום TLS** | VM ענן | nginx reverse proxy | ✅ תצורה |

### 6.2 מבנה Monorepo (מוצע)

```
tactitok/
├── packages/
│   ├── client/          ← Edge SPA (React/TS)
│   ├── admin/           ← Admin SPA (React/TS)
│   ├── server/          ← API Server (Node/Express/TS)
│   ├── shared/          ← טיפוסים משותפים, קבועים, אימות
│   └── edge-proxy/      ← Dockerfile + nginx.conf לפרוקסי מטמון קצה
├── package.json         ← שורש workspace
├── tsconfig.base.json   ← תצורת TS משותפת
└── ...
```

**נימוק:** Monorepo עם חבילות workspace (npm/yarn/pnpm workspaces) מאפשר לצוות לשתף טיפוסי TypeScript בין לקוח לשרת, להריץ צינור CI יחיד, ולהימנע מסטייית גרסאות. חבילת `shared` מכילה טיפוסי request/response של API ולוגיקת אימות בשימוש של כל שלוש החבילות. חבילת `edge-proxy` אינה חבילת TS אך חיה ב-monorepo לתצורת פריסה ממוקמת-יחד.

---

## 7. אחריות רכיבים

### C1 — Edge SPA

| אחריות | פרטים |
|--------|-------|
| **פיד ריילס** | גלילה אנכית בסגנון TikTok; השמעה-אוטומטית; ניווט-החלקה; סינון עניינים |
| **תצוגת ספרייה** | עץ קטגוריות 2-רמתי; חיפוש טקסט; רשימת תוכן עם מחווני סוג |
| **נגן וידאו** | אלמנט HTML5 `<video>`; פקדי נגן/עצור, סרגל התקדמות, השתק |
| **מציג PDF** | אינטגרציה PDF.js; ניווט עמוד-אחר-עמוד |
| **מנהל הורדות** | אתחול הורדות (מפעיל שמירה במטמון פרוקסי); הצגת התקדמות; ניהול פריטים שהורדו |
| **פרופיל מכשיר** | בחירת עניינים (ראשונה + הגדרות); שמירה ב-IndexedDB |
| **פעולות מקומיות** | לייק/שמירה נשמרים ב-IndexedDB; ללא סנכרון שרת ב-MVP |
| **טיפול מטה-נתונים** | בקשת קטלוג מפרוקסי קצה; שמירה ב-IndexedDB לסינון/חיפוש בצד לקוח |
| **מודעות לרשת** | זיהוי אונליין/אופליין (נגישות פרוקסי לענן); הצגת מחוון מצב; שחיקה חלקה |
| **ניווט** | סרגל לשוניות תחתי: ריילס, ספרייה, הורדות |

**מה השתנה (v0.2):** Edge SPA אינו עוד מנהל Cache API לקבצי מדיה או Service Worker לשמירת מעטפת SPA במטמון. כל שמירת תוכן במטמון מטופלת על ידי פרוקסי הקצה. ה-SPA שולח כל הבקשות ל-`localhost` (הפרוקסי), לעולם לא ישירות לענן. IndexedDB עדיין משמש לפרופיל מכשיר, פעולות מקומיות, רשומות הורדה (מטה-נתונים בלבד), וסנפשוט קטלוג מנותח לסינון בצד לקוח.

### C2 — Admin SPA

| אחריות | פרטים |
|--------|-------|
| **אימות** | מסך כניסה בסיסמה; token פעלה נשמר בזיכרון/sessionStorage |
| **ניהול תוכן** | העלאת וידאו (MP4) / PDF; מילוי טופס מטה-נתונים; עריכה; מחיקה |
| **עדכון תוכן** | החלפת קובץ לתוכן קיים; השרת מגדיל גרסה |
| **ניהול קטגוריות** | CRUD לעץ קטגוריות 2-רמתי |
| **ניהול עניינים** | CRUD לרשימת עניינים/תגיות שטוחה |
| **רשימת תוכן** | טבלה ניתנת-למיון, ניתנת-לסינון של כל פריטי התוכן |
| **אימות העלאה** | בצד לקוח: בדיקת סוג קובץ, גודל (≤100MB); השרת גם מאמת |

*(ללא שינויים מ-v0.1)*

### C3 — שרת API (Node.js + Express)

| אחריות | פרטים |
|--------|-------|
| **Content API** (פונה-לקצה) | `GET` מטה-נתוני קטלוג; `GET` קובץ תוכן (וידאו/PDF) עם תמיכת range request |
| **Admin API** (פונה-לניהול) | `POST` העלאה; `PUT` עדכון מטה-נתונים/קובץ; `DELETE` תוכן; CRUD קטגוריות; CRUD עניינים |
| **Auth middleware** | נקודות קצה אדמין מוגנות בפעלת סיסמה; נקודות קצה קצה הן ציבוריות |
| **הגשת קבצים סטטיים** | הגשת חבילת Admin SPA בלבד (Edge SPA נארז ב-Docker) |
| **פרוקסי תוכן** | הגשת קבצי תוכן ממאגר הקבצים עם headers נכונים (Content-Type, Accept-Ranges, Content-Length, Cache-Control) |
| **אימות** | דחיית וידאו שאינו MP4, מסמכים שאינם PDF, קבצים >100MB, וידאו >3 דקות (אם בדיקת משך מיושמת) |

**מה השתנה (v0.2):** שרת ה-API אינו עוד מגיש את חבילת Edge SPA. הוא מגיש רק את Admin SPA. מכשירי קצה מקבלים את ה-SPA מ-Docker container המקומי.

### C4 — שכבת שירותים

| שירות | אחריות |
|-------|--------|
| **CatalogService** | CRUD למטה-נתוני תוכן; חיפוש (כותרת + תיאור); סינון לפי קטגוריה/עניין |
| **CategoryService** | CRUD לעץ קטגוריות; אכיפת עומק מקסימלי 2; סידור מחדש |
| **InterestService** | CRUD לתגיות עניינים שטוחות |
| **ContentFileService** | אחסון/אחזור/מחיקת קבצים דרך הפשטת אחסון; יצירת content-id |
| **AuthService** | אימות סיסמת אדמין; הנפקה/אימות של tokens פעלה |
| **SyncService** | הגשת סנפשוט קטלוג מלא לבקשות סנכרון קצה |

*(ללא שינויים מ-v0.1)*

### C5 — PostgreSQL

| אחריות | פרטים |
|--------|-------|
| **אחסון מטה-נתונים** | פריטי תוכן, קטגוריות, עניינים, מיפויי תוכן-קטגוריה/עניין |
| **ניהול schema** | schema מבוסס-migrations (למשל, שימוש ב-Knex, Prisma, או SQL migrations גולמי) |
| **תמיכת query** | חיפוש טקסט (PostgreSQL `ILIKE` או `tsvector`); queries עץ קטגוריות; סינון עניינים |

*(ללא שינויים מ-v0.1)*

### C6 — מאגר קבצים (הפשטת אחסון)

| אחריות | פרטים |
|--------|-------|
| **ממשק** | `store(contentId, file) → path`; `retrieve(contentId) → stream`; `delete(contentId)` |
| **יישום MVP** | מערכת קבצים מקומית: קבצים נשמרים ב-`./data/content/{contentId}.{ext}` |
| **החלפה עתידית** | הממשק מאפשר החלפה ב-S3/MinIO ללא שינוי שכבת שירותים |
| **הגשה** | שרת API קורא קובץ ומסטרם ללקוח (לא גישה ישירה למערכת קבצים) |

*(ללא שינויים מ-v0.1)*

### C7 — פרוקסי קצה (nginx caching reverse proxy ב-Docker)

| אחריות | פרטים |
|--------|-------|
| **הגשת SPA** | הגשת קבצים סטטיים של Edge SPA (HTML/JS/CSS) מתמונת Docker; nginx `try_files` מטפל ב-SPA client-side routing |
| **פרוקסי API** | העברת כל בקשות `/api/*` לשרת הענן (HTTPS) |
| **שמירת תוכן במטמון** | שמירת תגובות API וקבצי תוכן (וידאו/PDF) ב-nginx `proxy_cache`; גודל מטמון ניתן-לתצורה (≥10 GB לדמו) |
| **שמירת מטה-נתונים במטמון** | שמירת תגובות API קטלוג במטמון; הגשת גרסה שמורה כשהענן אינו נגיש |
| **מצב אופליין** | כשהענן מושבת, הגשת כל התגובות השמורות באופן שקוף; Chrome/SPA לא צריך לדעת |
| **headers מטמון** | הוספת header `X-Cache-Status` (HIT/MISS/STALE) כך שה-SPA יכול לזהות רעננות |
| **לא אחראי על** | פרופיל מכשיר, פעולות מקומיות, מעקב הורדות (אלו נשארים ב-IndexedDB של Chrome) |

**תצורת nginx proxy_cache (מושגית):**
```
proxy_cache_path /var/cache/nginx levels=1:2
    keys_zone=tactitok:10m
    max_size=10g
    inactive=30d
    use_temp_path=off;

# שמירת קבצי תוכן אגרסיבית (גדולים, משתנים לעתים נדירות)
location /api/content/ {
    proxy_pass https://CLOUD_SERVER;
    proxy_cache tactitok;
    proxy_cache_valid 200 30d;
    proxy_cache_use_stale error timeout updating http_502 http_503 http_504;
    add_header X-Cache-Status $upstream_cache_status;
}

# שמירת מטה-נתוני קטלוג עם תוקף קצר יותר
location /api/catalog {
    proxy_pass https://CLOUD_SERVER;
    proxy_cache tactitok;
    proxy_cache_valid 200 5m;
    proxy_cache_use_stale error timeout updating http_502 http_503 http_504;
    add_header X-Cache-Status $upstream_cache_status;
}

# SPA fallback routing
location / {
    root /var/www/spa;
    try_files $uri $uri/ /index.html;
}
```

### C8 — סיום TLS (ענן)

| אחריות | פרטים |
|--------|-------|
| **גישת MVP** | nginx reverse proxy על פורט 443 → מעביר ל-Node.js על localhost:3000 |
| **גיבוי** | Node.js native HTTPS (הגדרה פשוטה יותר, פחות מוכן-לייצור) |
| **אישור** | Let's Encrypt (אם דומיין זמין) או חתום-עצמי לדמו |

*(ללא שינויים מ-v0.1)*

---

## 8. תצוגת זמן-ריצה / פריסה

### 8.1 פריסת VM ענן

```
Cloud VM (Linux, e.g., Ubuntu 22.04)
├── nginx (port 443, TLS termination)
│   └── proxy_pass → localhost:3000
├── Node.js process (port 3000)
│   ├── Express app
│   ├── Serves: Content API, Admin API, Admin SPA
│   └── Reads/writes: ./data/content/
├── PostgreSQL (port 5432, localhost only)
│   └── Database: tactitok
└── Filesystem
    └── ./data/content/  (uploaded files)
```

### 8.2 פריסת מכשיר קצה

```
Edge Device (Windows PC with 10″ tablet display)
│
├── WINDOWS SIDE
│   └── Chrome (kiosk mode)
│       ├── Navigates to http://localhost:8080
│       ├── IndexedDB: device profile, download records, local actions
│       └── All requests go to localhost:8080 (edge proxy)
│
└── LINUX VM (e.g., WSL2 or VirtualBox)
    └── Docker
        └── Container: tactitok-edge-proxy
            ├── nginx (port 8080, HTTP)
            │   ├── Serves: Edge SPA static files from /var/www/spa/
            │   ├── Proxies: /api/* → https://CLOUD_SERVER
            │   ├── Cache: /var/cache/nginx/ (content + metadata)
            │   └── Offline: serves cached responses when cloud is unreachable
            └── /var/www/spa/ (Edge SPA build files, baked into image)
```

**תהליך בנייה ב-Docker:**
1. `packages/client/` נבנה → מייצר `dist/` (קבצים סטטיים)
2. Dockerfile מעתיק `dist/` ל-`/var/www/spa/` ו-`nginx.conf` ל-`/etc/nginx/`
3. תוצאה: תמונת Docker יחידה (`tactitok-edge-proxy`) המכילה nginx + SPA

### 8.3 מודל תהליכים

**VM ענן:**

| תהליך | כמות | אסטרטגיית restart |
|-------|------|------------------|
| nginx (TLS) | 1 | שירות systemd |
| Node.js (שרת API) | 1 | שירות systemd או pm2 |
| PostgreSQL | 1 | שירות systemd |

**מכשיר קצה (לכל מכשיר):**

| תהליך | כמות | אסטרטגיית restart |
|-------|------|------------------|
| Docker daemon | 1 | systemd (ב-Linux VM) |
| nginx (בcontainer) | 1 | מדיניות restart של Docker: `always` |

**סך הכל: 3 תהליכים בענן + 2 תהליכים לכל מכשיר קצה.**

### 8.4 מודל רשת

```
Chrome (Windows) ──HTTP──► localhost:8080 (nginx in Docker/Linux VM)
                                │
                                │ HTTPS (when cloud reachable)
                                ▼
                          Cloud Server:443 (nginx TLS → Node.js:3000)

HQ Desktop (browser) ──HTTPS──► Cloud Server:443
```

- Chrome ↔ פרוקסי קצה: **HTTP** דרך localhost (ללא TLS — אותו מחשב; ללא חשיפת רשת)
- פרוקסי קצה ↔ ענן: **HTTPS** (TLS נדרש; פרוקסי קצה מאמת אישור שרת ענן)
- שולחן עבודה מטה ↔ ענן: **HTTPS** (TLS דפדפן רגיל)

---

## 9. סקירת זרימת נתונים / תוכן

### 9.1 זרימת העלאת תוכן

```
HQ Staff → Admin SPA → POST /api/admin/content (multipart) → API Server
    → אמת (סוג, גודל, פורמט)
    → ContentFileService.store(file) → ./data/content/{id}.{ext}
    → CatalogService.create(metadata) → PostgreSQL
    → החזר 201 + מטה-נתוני תוכן
```

*(ללא שינוי מ-v0.1 — העלאות עוברות ישירות לענן משולחן עבודה מטה)*

### 9.2 זרימת סנכרון מטה-נתונים (קצה)

```
Edge SPA (בפתיחת אפליקציה / רענון ידני)
    → GET /api/catalog → localhost:8080 (פרוקסי קצה)
    → פרוקסי קצה בודק מטמון:
        → MISS או STALE: העברה לשרת ענן → שמירה במטמון → החזרה
        → HIT: הגשה ממטמון
        → ענן לא נגיש: הגשת מטמון ישן (proxy_cache_use_stale)
    → Edge SPA מקבל catalog JSON
    → שומר ב-IndexedDB לסינון/חיפוש בצד לקוח
    → ממשק מוצג מנתוני קטלוג מנותחים
```

### 9.3 זרימת צריכת תוכן (וידאו)

```
Edge SPA (משתמש גולל לוידאו בריילס)
    → <video src="/api/content/{id}/file">
    → דפדפן שולח GET עם Range header ל-localhost:8080
    → פרוקסי קצה:
        → אם שמור במטמון: הגשה מ-/var/cache/nginx/ (HIT)
        → אם לא שמור: פרוקסי לענן, שמירת תגובה מלאה במטמון, סטרימינג ל-Chrome
    → דפדפן מנגן וידאו בצורה פרוגרסיבית
```

**הערה:** nginx `proxy_cache` שומר את התגובה המלאה בבקשה הראשונה. בקשות עוקבות (כולל range requests לאותו קובץ) מוגשות ממטמון. משמעות הדבר שנגינה ראשונה של וידאו גם שומרת אותו מראש לשימוש אופליין.

### 9.4 זרימת צריכת תוכן (PDF)

```
Edge SPA (משתמש פותח PDF מהספרייה)
    → fetch("/api/content/{id}/file") → localhost:8080
    → פרוקסי קצה מגיש ממטמון או מפרוקסי לענן (כמו וידאו)
    → Edge SPA מעביר ArrayBuffer ל-PDF.js
    → PDF.js מציג עמוד-אחר-עמוד ב-canvas
```

### 9.5 זרימת הורדה

```
Edge SPA (משתמש מקיש הורדה)
    → fetch("/api/content/{id}/file") → localhost:8080
    → פרוקסי קצה:
        → אם שמור: הגשה מיידית (HIT)
        → אם לא שמור: פרוקסי לענן → שמירה → סטרימינג ל-Chrome
    → SPA מקבל תגובה מלאה (קורא עד השלמה כדי להבטיח שמירה בפרוקסי)
    → SPA שומר רשומת הורדה ב-IndexedDB (id, title, type, size, date, version)
    → ממשק מתעדכן: פריט מופיע בלשונית הורדות
```

**מה השתנה (v0.2):** ה-SPA אינו עוד שומר קובץ ב-Cache API. הקובץ שמור על ידי פרוקסי הקצה. ה-SPA שומר רק רשומת מטה-נתונים ב-IndexedDB למעקב אחר מה שהמשתמש הוריד במפורש. מטמון הפרוקסי מחזיק את הקובץ בפועל.

### 9.6 זרימת נגינה אופליין

```
Edge SPA (משתמש פותח פריט, ענן לא נגיש)
    → קריאת רשומת הורדה מ-IndexedDB (מטה-נתונים בלבד)
    → בקשת קובץ: <video src="/api/content/{id}/file"> או fetch(...)
    → בקשה עוברת ל-localhost:8080 (פרוקסי קצה)
    → פרוקסי קצה: ענן לא נגיש → הגשה ממטמון (STALE)
    → דפדפן מנגן וידאו / SPA מציג PDF כרגיל
```

**מה השתנה (v0.2):** נגינה אופליין היא שקופה. ה-SPA מגיש אותה בקשה כמו אונליין; הפרוקסי מטפל בהגשה אופליין. ללא נתיב קוד אופליין מיוחד ב-SPA (ללא cache API lookups, ללא blob URLs). קוד ה-SPA לנגינה אונליין ואופליין זהה.

### 9.7 זרימת טעינה מוקדמת (ריילס)

```
Edge SPA (בזמן שהוידאו הנוכחי מנוגן)
    → זיהוי הוידאו הבא בפיד
    → fetch("/api/content/{nextId}/file", { headers: { Range: "bytes=0-{N}" } })
    → בקשה עוברת ל-localhost:8080 (פרוקסי קצה)
    → פרוקסי: אם לא שמור, שולף קובץ מלא מהענן ושומר
    → מחזיר תגובה חלקית ל-Chrome
    → שמירת תגובה חלקית בזיכרון
    → כשמשתמש מחליק לוידאו הבא:
        → אתחול נגינה מה-bytes שנטענו מוקדם
        → המשך דרך בקשות נוספות (מוגשות ממטמון פרוקסי — מיידי)
```

**יתרון הפרוקסי:** בקשת הטעינה המוקדמת מפעילה את הפרוקסי לשמור את הקובץ המלא. בקשות range עוקבות לאותו קובץ מוגשות ממטמון עם latency אפס.

---

## 10. גישת אופליין / סנכרון / מתמידות מקומית

### 10.1 הקצאת אחסון

| מאגר | מיקום | תכנים | ציפיית גודל | מתמידות |
|------|-------|--------|------------|--------|
| **מטמון פרוקסי קצה** | מערכת קבצים Docker (Linux VM) | קבצי תוכן שמורים + תגובות API | עד 10 GB | נמשך עד ניקוי Docker volume או פינוי מטמון |
| **IndexedDB** | Chrome (Windows) | פרופיל מכשיר (עניינים), רשומות הורדה (מטה-נתונים), פעולות מקומיות (לייק/שמירה) | <1 MB | נמשך בין הפעלות |
| **זיכרון** | Chrome (Windows) | חיצוני טעינה מוקדמת לוידאו הבא; נתוני קטלוג מנותחים | ~5–20 MB | אובד בסגירת דף |

**מה השתנה (v0.2):** Cache API ו-Service Worker cache אינם בשימוש עוד. `proxy_cache` של פרוקסי הקצה מחליף את שניהם. זה מבטל את כל קוד Service Worker וקוד Cache API מה-SPA, ומפשט משמעותית את הלקוח.

### 10.2 מטריצת יכולת אופליין

| תכונה | אונליין | אופליין (ענן לא נגיש) | כיצד |
|-------|---------|---------------------|------|
| אתחול אפליקציה | טעינת SPA מפרוקסי | ✅ SPA מקומי (ארוז ב-Docker) | nginx מגיש קבצים סטטיים מ-`/var/www/spa/` |
| פיד ריילס | סטרימינג דרך פרוקסי → ענן | ✅ אם נגיש בעבר | פרוקסי מגיש קבצי וידאו שמורים |
| עיון בספרייה | קטלוג טרי מענן דרך פרוקסי | ✅ קטלוג שמור | פרוקסי מגיש תגובת `/api/catalog` שמורה |
| נגינת וידאו | סטרימינג דרך פרוקסי → ענן | ✅ אם נגיש בעבר או הורד | פרוקסי מגיש קובץ שמור |
| צפיית PDF | שליפה דרך פרוקסי → ענן | ✅ אם נגיש בעבר או הורד | פרוקסי מגיש קובץ שמור |
| לשונית הורדות | הצגת רשומות הורדה | ✅ תמיד | רשומות IndexedDB; קבצים מוגשים ממטמון פרוקסי |
| שינוי עניינים | מיידי | ✅ מקומי בלבד | כתיבה ל-IndexedDB |
| סנכרון מטה-נתונים | פרוקסי מעביר לענן | ❌ הגשת מטמון ישן | הצגת חותמת "סונכרן לאחרונה"; פרוקסי מגיש ישן |
| חיפוש | סינון בצד לקוח על קטלוג | ✅ על קטלוג שמור | סנפשוט קטלוג IndexedDB |

**שיפור מרכזי (v0.2):** פיד ריילס אופליין אפשרי כעת לוידאו שנגיש בעבר. ב-v0.1, ריילס דרש שידור חי. כעת מטמון הפרוקסי הופך כל וידאו שנצפה בעבר לזמין אופליין.

### 10.3 אסטרטגיית סנכרון

- **כיוון:** פרוקסי קצה → pull ענן (שקוף; פרוקסי קצה מעביר בקשות)
- **טריגר:** בפתיחת אפליקציה + כפתור רענון ידני (SPA מבקש `/api/catalog`)
- **מטען:** קטלוג JSON מלא (כל מטה-נתוני תוכן)
- **פתרון התנגשויות:** השרת מנצח; Edge SPA מחליף קטלוג מקומי ב-IndexedDB
- **ישנות:** פרוקסי קצה שומר קטלוג ~5 דקות; אחרי כן, הבקשה הבאה עוברת לענן (אם נגיש)
- **סנכרון מצטבר:** לא ב-MVP (הקטלוג קטן: ~15 פריטים). עיצוב API לתמוך בפרמטר `?since=timestamp` לסנכרון דלתא עתידי.

### 10.4 ניהול מטמון פרוקסי קצה

| הגדרה | ערך | נימוק |
|-------|-----|-------|
| **גודל מטמון מקסימלי** | 10 GB | כל תוכן הדמו (~15 פריטים × ≤100 MB) מתאים בנוחות |
| **תוקף מטמון קבצי תוכן** | 30 יום | תוכן משתנה לעתים נדירות; בדיקת גרסה בסנכרון מטה-נתונים מזהה עדכונים |
| **תוקף מטמון קטלוג** | 5 דקות | תכוף מספיק לדמו; הגשה ישנה כשאופליין |
| **פינוי לא-פעיל** | 30 יום | פריטים שלא נגישים ל-30 יום מפונים; נדיב לדמו |
| **הגשה ישנה** | בשגיאה, timeout, 502, 503, 504 | מבטיח פונקציונליות אופליין |

---

## 11. שיקולי אבטחה / אמון / תפעול

### 11.1 אבטחת תעבורה

- ענן: כל התעבורה דרך HTTPS (TLS 1.2+); nginx מסיים TLS על פורט 443
- פרוקסי קצה → ענן: HTTPS (פרוקסי מאמת אישור שרת ענן)
- Chrome → פרוקסי קצה: HTTP דרך localhost (ללא TLS — אותו מחשב; ללא חשיפת רשת)
- אישור חתום-עצמי או Let's Encrypt לשרת ענן בדמו

### 11.2 אימות

| משטח | מנגנון | יישום |
|------|--------|-------|
| Edge SPA → פרוקסי קצה → Content API | ללא (ציבורי, קריאה-בלבד) | ללא auth middleware ב-`/api/catalog`, `/api/content` |
| Admin SPA → Admin API | סיסמה משותפת | `POST /api/admin/login` → מחזיר token פעלה (JWT או opaque); token נשלח ב-header `Authorization` |
| פעלת אדמין | אימות בצד שרת | תפוגת token; נשמר ב-sessionStorage (לא localStorage — מתנקה בסגירת כרטיסייה) |

### 11.3 אבטחת תוכן

- קבצי תוכן מוגשים על ידי שרת ה-API — ללא כתובות URL ישירות למערכת קבצים חשופות
- מטמון פרוקסי קצה נמצא במערכת קבצים Linux VM המקומית — לא נגיש ישירות מ-Windows (Chrome ניגש דרך HTTP בלבד)
- העלאת קבצים מאמת MIME type + סיומת קובץ
- לא מתקבלים סוגי קבצים הניתנים להרצה
- קבצים שהועלו נשמרים עם שמות content-id שנוצרו (ללא נתיבים שסופקו על ידי משתמש)

### 11.4 יסודות תפעוליים

- לוגינג שרת רגיל (לוגי בקשות, לוגי שגיאות) — ללא פלטפורמת אנליטיקה
- connection pooling ל-PostgreSQL (גודל pool ברירת מחדל מספיק ל-~20 במקביל)
- pm2 או systemd לrestart אוטומטי של תהליך Node.js
- מדיניות Docker restart `always` ל-container פרוקסי קצה
- ללא פלטפורמת ניטור/התראה ב-MVP

---

## 12. תלויות חיצוניות

| תלות | גרסה/מפרט | תפקיד | סיכון |
|------|-----------|-------|-------|
| **Node.js** | 20 LTS | סביבת ריצה שרת API (ענן) | נמוך — LTS יציב |
| **PostgreSQL** | 15+ | אחסון מטה-נתונים (ענן) | נמוך — סטנדרטי |
| **nginx** | יציב אחרון | סיום TLS (ענן) + פרוקסי מטמון (קצה) | נמוך — סטנדרטי |
| **React** | 18+ | מסגרת ממשק (קצה + ניהול) | נמוך — אקוסיסטם דומיננטי |
| **TypeScript** | 5+ | בטיחות טיפוסים בכל ה-stack | נמוך — סטנדרטי |
| **PDF.js** | אחרון | עיצוב PDF בדפדפן | נמוך — מתוחזק על ידי Mozilla |
| **Express** | 4.x | מסגרת שרת HTTP | נמוך — בשל |
| **npm/pnpm workspaces** | אחרון | ניהול חבילות monorepo | נמוך |
| **Docker** | יציב אחרון | סביבת ריצה container פרוקסי קצה | נמוך — סטנדרטי |
| **Chrome** | יציב אחרון | סביבת ריצה קצה (Windows) | חיצוני — לא מנוהל על ידי הצוות |

ללא תלויות זמן-ריצה בשירותי ענן (AWS, GCP, וכו') — שרת הענן רץ על Linux VM רגיל. מכשירי קצה מריצים Docker ב-Linux VM המקומי שלהם.

---

## 13. החלטות ארכיטקטורה מרכזיות

| # | החלטה | נימוק | חלופות שנשקלו |
|---|-------|-------|--------------|
| AD1 | **TypeScript monorepo** (client + admin + server + shared + edge-proxy) | שפה אחת; טיפוסים משותפים מונעים סטייה בחוזה API; CI פשוט | repos נפרדים (קשה יותר לשתף טיפוסים); JS ללא TS (פחות בטוח) |
| AD2 | **React לשני ה-SPAs** | אקוסיסטם הגדול ביותר; תמיכה הטובה ביותר לדפוסי scroll-snap/TikTok; סטודנטים ככל הנראה מכירים | Vue (פשוט יותר אך אקוסיסטם קטן יותר); Svelte (חדש יותר, פחות משאבים) |
| AD3 | **Node.js + Express backend** | אותה שפה כמו frontend; סטרימינג קבצים פשוט; נאות ל-~20 לקוחות | Python/FastAPI (שפה שונה); Go (מוגזם ל-MVP) |
| AD4 | **PostgreSQL למטה-נתונים** | כיתה-ייצורית; תומך בחיפוש טקסט; מטפל בקריאות במקביל; מוכן-להמשך | SQLite (פשוט יותר אך concurrency גרוע); MongoDB (פחות מובנה) |
| AD5 | **מערכת קבצים מקומית לקבצי תוכן** מאחורי הפשטה | הפשוט ביותר; ללא שירות חיצוני; הפשטה מאפשרת החלפה ב-S3/MinIO | S3 ישירות (מוסיף תלות ענן); MinIO (תהליך נוסף) |
| AD6 | **MP4 + HTTP range requests** לוידאו | הסטרימינג הפשוט ביותר; ללא עיבוד; Chrome מטפל באופן נייטיב | HLS/DASH (מורכב; צריך segmenting); WebRTC (מקרה שימוש שגוי) |
| AD7 | **פרוקסי קצה (nginx ב-Docker) להגשת SPA + שמירה במטמון** | מבטל מורכבות Service Worker ו-Cache API; מטפל באופליין שקוף; קוד SPA פשוט יותר | Service Worker + Cache API (מורכבות לקוח רבה יותר); פרוקסי Node.js מותאם בקצה (קוד רב יותר לתחזוקה) |
| AD8 | **IndexedDB למצב מכשיר בלבד** (פרופיל, פעולות, רשומות הורדה) | רק נתונים מובנים מקומיים ש-SPA צריך לנהל ישירות; כל שמירת תוכן במטמון מואצלת לפרוקסי | IndexedDB + Cache API (מיותר עם פרוקסי); localStorage (מוגבל בגודל) |
| AD9 | **תהליך Node.js יחיד** (ללא microservices) | 3 מפתחים; 10 שבועות; ~20 משתמשים; ללא צורך בארכיטקטורה מבוזרת | Microservices (מורכב מדי); serverless (מודל פריסה שגוי) |
| AD10 | **nginx כ-TLS reverse proxy** (ענן) | דפוס סטנדרטי; מוריד TLS מ-Node; מוכן-לייצור | Node native TLS (פשוט יותר אך פחות חזק); Caddy (פחות נפוץ) |
| AD11 | **סנכרון קטלוג מלא** (לא דלתא) | הקטלוג ~15 פריטים; מטען JSON מלא <10 KB; דלתא מוסיף מורכבות | סנכרון דלתא (אופטימיזציה מוקדמת מדי); GraphQL subscriptions (מוגזם) |
| AD12 | **SPA ארוז בתמונת Docker** (לא נשלף מהענן) | SPA תמיד זמין מקומית; ללא תלות בענן לאתחול אפליקציה; מפשט אופליין | הגשת SPA מענן (דורש SW לאתחול אופליין); התקנת SPA מראש על Windows (קשה יותר לעדכון) |

---

## 14. חלופות שנשקלו או נדחו בכוונה

| חלופה | מדוע נדחתה / נדחתה |
|-------|-------------------|
| **HLS/DASH adaptive streaming** | דורש tooling segmentation וידאו (ffmpeg); MP4 range requests מספיקים לקטעים ≤3 דקות ב-~20 מכשירים |
| **Service Worker לשמירת SPA במטמון** | SPA ארוז מקומית ב-Docker; SW מוסיף מורכבות ללא יתרון בטופולוגיה זו |
| **Cache API לקבצי מדיה** | מטמון פרוקסי קצה מטפל בכל שמירת תוכן; Cache API יהיה מיותר |
| **GraphQL** | REST פשוט יותר; הקטלוג קטן; GraphQL מוסיף עקומת למידה |
| **WebSocket / SSE לסנכרון דחיפה** | סנכרון מבוסס-pull מספיק ל-MVP; דחיפה ניתן להוספה לאותו שרת Express מאוחר יותר |
| **שכבת מטמון Redis** | ~20 מכשירים במקביל לא דורשים מטמון בזיכרון; PostgreSQL מטפל בכך |
| **CDN למשלוח תוכן** | שרת יחיד עם הגשת קבצים ישירה מספיק; CDN ניתן להוספה מול אותו API מאוחר יותר |
| **React Native / PWA installable** | Chrome קיוסק הוא היעד; ללא צורך בהתקנה נייטיב |
| **ORM (Sequelize, TypeORM)** | query builder (Knex) או ORM קל (Prisma) עדיף; ORM כבד מוסיף הפשטה ללא יתרון |
| **S3 / MinIO לאחסון תוכן** | מערכת קבצים מקומית הפשוטה ביותר ל-MVP; הפשטת אחסון מאפשרת החלפה מאוחר יותר |
| **Microservices** | 3 מפתחים, 10 שבועות, ~20 משתמשים — monolith עם מודולים פנימיים נקיים הוא הבחירה הנכונה |
| **פרוקסי Node.js מותאם בקצה** | nginx proxy_cache הוא שמירה במטמון בלי-קוד בוגרת; פרוקסי מותאם פירושו קוד רב יותר לתחזוקה |
| **Kubernetes / Docker Compose orchestration** | container יחיד לכל מכשיר קצה; `docker run` עם מדיניות restart מספיק |

---

## 15. הנחות

| # | הנחה | השפעה אם שגויה |
|---|------|----------------|
| AA1 | React + TypeScript נגיש ל-3 מפתחי הסטודנטים | נדרשת מסגרת חלופית; עיכובים בתחילת פרויקט |
| AA2 | Node.js + Express מטפל ב-~20 שידורי וידאו במקביל ללא צוואר-בקבוק | נדרשת בדיקת עומסים; ייתכן שנדרשים worker threads או nginx לקבצים |
| AA3 | PostgreSQL רץ בנוחות לצד Node.js + nginx על VM יחיד (4+ GB RAM) | נדרש VM גדול יותר או אופטימיזציית משאבים |
| AA4 | npm/pnpm workspaces monorepo ניתן לניהול ל-3 סטודנטים | ייתכן שנדרשים repos נפרדים אם tooling workspace גורם חיכוך |
| AA5 | סטודנטים יכולים לכתוב Dockerfile + nginx.conf לפרוקסי קצה עם הדרכה סבירה | ייתכן שנדרש template מוכן-מראש; מוסיף ~1–2 ימים אם לא מכירים |
| AA6 | Express יכול להגיש קבצי תוכן עם תמיכת range request (206 Partial Content) בצורה נאותה | ייתכן שnginx יצטרך להגיש קבצים ישירות מדיסק (עוקף Node) |
| AA7 | תהליך Node.js יחיד (לא clustered) מטפל בעומס MVP | ייתכן שנדרש מצב cluster של pm2 או איזון nginx upstream |
| AA8 | nginx proxy_cache מטפל נכון ב-range requests עבור תוכן שמור | נדרשת בדיקה; אם לא, ייתכן שנדרש שליפת קובץ-מלא לשמירה ואז range serving נפרד |
| AA9 | רשת Linux VM מאפשרת ל-Docker container לחבור פורט 8080 נגיש מ-Windows localhost | ייתכן שנדרשת תצורת port forwarding או bridge networking |
| AA10 | מטמון פרוקסי 10 GB מספיק לכל תוכן הדמו (~15 פריטים) | הגדלת גודל מטמון או הוספת ניטור פינוי |

---

## 16. סיכונים

| # | סיכון | סבירות | השפעה | הפחתה |
|---|-------|---------|-------|-------|
| AR1 | Node.js הופך לצוואר-בקבוק בהגשת קבצי וידאו ל-20 לקוחות במקביל | בינונית | גבוהה | בדיקה מוקדמת עם עומס מדומה; גיבוי: nginx מגיש קבצים ישירות מ-`./data/content/` |
| AR2 | הגדרת Docker / Linux VM לא מוכרת לסטודנטים ולוקחת יותר מדי זמן | בינונית | בינונית | ספקת template Dockerfile מוכן-מראש; שמירת תצורה מינימלית; pair-program הגדרה |
| AR3 | tooling workspace monorepo גורם חיכוך למפתחים | נמוכה | בינונית | גיבוי ל-repos נפרדים עם טיפוסים משותפים דרך npm package; מוסיף יום הגדרה |
| AR4 | חיפוש טקסט ב-PostgreSQL לא מספיק לאיכות דמו | נמוכה | נמוכה | שימוש ב-`ILIKE` ל-MVP; שדרוג ל-`tsvector` אם נדרש; או סינון בצד לקוח (15 פריטים) |
| AR5 | React scroll-snap לריילס בסגנון TikTok גמגומי על טאבלט | בינונית | גבוהה | אב-טיפוס בשבוע 1; שקול react-virtualized או touch handler מותאם |
| AR6 | העלאת קובץ (100MB) איטית או timeout ברשת אדמין לא יציבה | נמוכה | בינונית | ספריית העלאה chunked (למשל, tus-js-client); או קבלה שאדמין משתמש ברשת מטה יציבה |
| AR7 | nginx proxy_cache מפנה תוכן שהורד לפני שמשתמש ניגש אופליין | נמוכה | בינונית | הגדרת inactive timeout נדיב (30d); ניטור hit rate מטמון; גודל מטמון 10 GB נאות ל-15 פריטים |
| AR8 | מיפוי פורטים בין Linux VM ל-Windows (עבור localhost:8080) דורש תצורה לא-טריוויאלית | בינונית | בינונית | בדיקת רשת VM מוקדמת (WSL2 ממפה פורטים אוטומטית; VirtualBox צריך port forwarding) |
| AR9 | nginx proxy_cache אינו שומר תגובות range request נכון במטמון | נמוכה | גבוהה | בדיקה מוקדמת; אם נדרש, תצורת פרוקסי לשלוף קובץ מלא תמיד ולהגיש ranges ממטמון |

---

## 17. שאלות פתוחות / החלטות ממתינות

| # | שאלה | משפיעה על | ברירת מחדל מומלצת | מועד אחרון |
|---|------|-----------|-------------------|-----------|
| AQ1 | שכבת ORM / query מדויקת: Prisma לעומת Knex לעומת SQL גולמי? | יישום | Prisma (בטיחות טיפוסים, תמיכת migration, DX טוב) | לפני תחילת יישום |
| AQ2 | האם nginx (ענן) צריך להגיש קבצי תוכן ישירות (עוקף Node) לביצועים? | פריסה | התחל עם Node; מעבר ל-nginx `X-Accel-Redirect` אם עומס הוא בעיה | ספרינט 2 (לאחר בדיקת עומס) |
| AQ3 | CSS scroll-snap לעומת ספרייה (למשל, Swiper.js) לריילס בסגנון TikTok? | יישום ממשק | CSS scroll-snap תחילה; הוספת Swiper אם גלילה גמגומית | אב-טיפוס ספרינט 1 |
| AQ4 | כלי monorepo: npm workspaces לעומת pnpm workspaces לעומת turborepo? | צינור בנייה | pnpm workspaces (מהיר, אמין, פשוט יותר מ-turborepo) | לפני תחילת יישום |
| AQ5 | סוג Linux VM במכשירי קצה: WSL2 לעומת VirtualBox לעומת אחר? | הגדרת קצה | WSL2 (אינטגרציה Docker פשוטה ביותר על Windows) | לפני הגדרת קצה |
| AQ6 | כתובת URL שרת ענן: hardcoded ב-nginx.conf או ניתן-לתצורה דרך environment variable? | פריסת פרוקסי קצה | Environment variable (Docker `-e CLOUD_URL=...`) | לפני יישום |
| AQ7 | האם פרוקסי הקצה צריך להתחבר לענן ב-HTTP או HTTPS? | אבטחה | HTTPS (TLS); פרוקסי מאמת אישור ענן | לפני פריסה |

---

## 18. מנופי צמצום היקף

| עדיפות | פישוט | השפעה |
|--------|-------|-------|
| 1 | השמטת TLS nginx בענן; שימוש ב-Node.js native HTTPS | פריסת ענן פשוטה יותר; טיפול TLS מעט פחות חזק |
| 2 | Hardcode כתובת URL ענן ב-nginx.conf (דילוג על env var) | הגדרת Docker פשוטה יותר; חייבים לבנות מחדש תמונה לשינויי URL |
| 3 | השמטת monorepo; שימוש ב-repos נפרדים ל-client/server | יותר חיכוך הגדרה; קשה יותר לשתף טיפוסים; אך מסיר מורכבות workspace |
| 4 | שימוש ב-SQLite במקום PostgreSQL | הגדרה פשוטה יותר; מקובל ל-~20 מכשירים; אובד חיפוש טקסט ו-concurrent writes |
| 5 | הגשת קבצי תוכן דרך nginx ישירות בענן (דילוג על פרוקסי Node) | אובד בקרת גישה מרוכזת; אך מסיר חשש צוואר-בקבוק Node |
| 6 | דילוג על Docker; התקנה ידנית של nginx + קבצי SPA על Linux VM | מסיר עקומת למידה Docker; קשה יותר לעדכן/לשחזר |

---

## 19. הערות להמשך

הנחיות לצוות ממשיך המרחיב מעבר לארכיטקטורת MVP:

- **מעבר ל-microservices:** שכבת השירותים (CatalogService, ContentFileService, וכו') היא מודולרית. כל שירות ניתן לחילוץ לתהליך נפרד עם ה-API שלו אם גדילה דורשת זאת. התחל בחילוץ ContentFileService אם עומס וידאו גדל.
- **אינטגרציית CDN:** פרוקסי התוכן (`/api/content/{id}/file`) ניתן לסינון ב-CDN. הוסף headers מטמון (`Cache-Control`, `ETag`) מהיום הראשון כך שאינטגרציית CDN היא שינוי תצורה, לא שינוי קוד.
- **סנכרון דחיפה (WebSocket/SSE):** שרת Express יכול להוסיף נקודת קצה WebSocket לצד REST. פרוקסי הקצה יעביר חיבורי WebSocket ללא שמירה במטמון. מודול הסנכרון של ה-SPA צריך להיות מעוצב עם ממשק `SyncProvider` כך שpull ניתן להחלפה ב-push.
- **אחסון אובייקטים (S3/MinIO):** להפשטת האחסון (`ContentFileService`) יש מתודות `store`, `retrieve`, `delete`. החלף את יישום מערכת הקבצים ב-S3 client תוך שמירת אותו ממשק.
- **אימות משתמשים:** הוסף passport.js או auth middleware דומה. ה-API כבר מפריד בין נתיבים ציבוריים (קצה) למוגנים (ניהול). עתידי: הוסף auth לנתיבי קצה, החלף device-id ב-user-id.
- **Clustering / load balancing:** מצב cluster pm2 או מספר instances Node מאחורי nginx upstream. PostgreSQL מטפל בחיבורים במקביל באופן נייטיב. מאגר הקבצים משותף דרך מערכת קבצים (או מאגר אובייקטים למולטי-VM).
- **CI/CD:** ה-monorepo תומך בצינור יחיד: lint → typecheck → test → build → build Docker image → deploy. הוסף GitHub Actions או דומה כשהצוות מוכן.
- **אסטרטגיית database migration:** שימוש ב-Prisma migrations (או Knex migrations). כל שינוי schema הוא קובץ migration בגרסה. זה קריטי-להמשך — חייב להיות מוגדר מספרינט 1.
- **שיפור פרוקסי קצה:** הוסף prefetch תוכן בצד-פרוקסי (background job שמושך כל תוכן חדש מהענן למטמון). כרגע הפרוקסי שומר בגישה ראשונה; prefetch יזום מבטיח שכל התוכן זמין אופליין לפני שהמשתמש מבקש אותו.
- **Service Worker (עתידי):** אם דרישות קצה גדלות מעבר למה שהפרוקסי מטפל (למשל, push notifications, background sync של פעולות מקומיות), Service Worker ניתן להוספה ל-SPA לצד הפרוקסי. הפרוקסי מספק את בסיס השמירה במטמון; SW יוסיף תכונות ברמת דפדפן.

---

*מסמך זה הוא הרביעי במערכת מסמכי TactiTok. המשך ל-`product/05_data-model.md`.*
